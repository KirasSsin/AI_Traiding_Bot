### [BLOCKER] TL-01 (trading-logic) — Live runtime never arms OCO bracket and drops all exit signals → open position has zero SL/TP and never closes on signal
**File:** src/runtime/manager.py:314-315, 353-358
**Detail:** _poll_bar_and_strategy entry path calls ONLY coordinator.start_bracket() (manager.py:353) which places the Market BUY entry leg and transitions FLAT→ENTRY_PENDING. The TP Limit + SL Stop-Market legs are designed to be armed AFTER fill via Coordinator.arm_oco(), but arm_oco() is NEVER called by any production code (grep src/**.py: only the def + docstrings/log strings, zero call sites). Coordinator.on_order_event entry-Filled branch (coordinator.py:307-312) only does _transition(ENTRY_FILLED)→LONG_OPEN then returns — it does NOT arm. The FSM has the (LONG_OPEN, TP_PLACED) transition row but nothing ever fires it. Separately, manager.py:314 `if signal is None or signal.side == SignalSide.FLAT: return` DROPS every EXIT_FLAT_* signal, and coordinator.flatten() is likewise never called from production (zero call sites in src). Net live behavior for the wired strategy (MeanReversionRsiBBStrategy, __main__.py:146): enter LONG → fill → LONG_OPEN → stays open forever with NO stop-loss, NO take-profit, NO signal-based exit. Confirmed by tests: arm_oco/flatten are exercised only when integration tests call them manually (tests/integration/test_demo_bracket_happy_path.py:182,190); no test asserts the runtime/event-loop arms automatically. The δ-TESTNET HaltGate (s35_demo_active) IS wired in _tick, so this runtime is intended for demo/testnet activation — if activated against any real account this is an unbounded-loss defect.
**Fix:** Wire the entry-fill → arm_oco edge and the exit-signal → flatten edge: (1) on ENTRY_FILLED (in Coordinator.on_order_event entry branch or via a RuntimeManager fill callback) call arm_oco(tp_price, sl_trigger_price, oco_qty) using the persisted assessment prices so LONG_OPEN→OCO_ARMING→OCO_ARMED actually places protective legs; (2) handle FLAT signals in _poll_bar_and_strategy by routing to coordinator.flatten(reason=ReasonCode(signal.reason)) when state is LONG_OPEN/OCO_ARMED; (3) wire reconcile_arming_ttl into the tick loop so stuck OCO_ARMING halts. Add an end-to-end test asserting start_bracket→entry-fill→arm_oco places TP+SL, and that an EXIT_FLAT signal flattens an open position. Ref: ADR 0020 (3-order OCO emulation), execution-timing.md production-implementation step (3) 'On entry-fill event: place OCO bracket'.

### [BLOCKER] BYBIT-01 (bybit-api) — REST and private WS connect to DIFFERENT Bybit environments (testnet exchange vs demo) — order→fill event loop broken
**File:** src/__main__.py:167 (+ src/marketdata/bybit/rest.py:127, src/execution/bybit/ws_private.py:76-82)
**Detail:** In the LIVE production run command `_cmd_run` (src/__main__.py:64-198, NOT a stub since S12), the private WS endpoint is built as `endpoint = "demo.bybit.com" if settings.testnet else "stream.bybit.com"` (line 167). ws_private.py:76-82 then resolves pybit flags as `testnet="testnet" in self._endpoint` (= False for "demo.bybit.com") and `demo="demo" in self._endpoint` (= True). So with settings.testnet=True the private WS connects to the Bybit DEMO environment (`wss://stream-demo.bybit.com`, mainnet-infra paper). Meanwhile the REST client is `HTTP(testnet=settings.testnet)` with NO demo flag (rest.py:127), which resolves to the TESTNET exchange (`https://api-testnet.bybit.com`). Verified against pybit 5.16.0 source: HTTP SUBDOMAIN_TESTNET="api-testnet" vs DEMO_SUBDOMAIN_MAINNET="api-demo"; WS SUBDOMAIN_TESTNET="stream-testnet" vs DEMO_SUBDOMAIN_MAINNET="stream-demo". testnet-exchange and demo are completely separate account universes. Orders placed via REST on api-testnet.bybit.com will NEVER produce order/execution/wallet events on stream-demo.bybit.com, so the FSM never receives ENTRY_FILLED/Triggered/Filled echoes; OCO never arms via the live path and the only state-advance is the reconcile REST sweep. The S35 demo runtime requires testnet=True (config.py:263 blocks s35_demo_active when not testnet), so this divergence is exactly the active demo configuration. Whatever the intended single environment (testnet OR demo), REST and WS currently disagree — that is the defect regardless of intent.
**Fix:** Pick ONE environment and route both clients to it consistently. Stop deriving pybit flags by substring-matching an ad-hoc endpoint string. Pass an explicit (testnet, demo) pair from Settings to BOTH BybitRESTClient (add a `demo` param → HTTP(testnet=..., demo=...)) and BybitPrivateWSConsumer (replace the `"testnet"/"demo" in endpoint` heuristic with explicit booleans). E.g. for the S35 demo runtime use testnet=False, demo=True for both REST and WS; for true testnet use testnet=True, demo=False for both. Add a startup assertion/log that prints the resolved REST host and WS host so a divergence is visible. Add a test asserting REST and WS resolve to the same Bybit environment for each Settings combination.

### [HIGH] ARCH-02 (architecture) — Blocking REST I/O with exponential-backoff sleep executes while holding both Coordinator RLock and Reconciler Lock on the safety-critical reconcile path
**File:** src/execution/coordinator.py:108-118 (on_ws_reconnect holds RLock -> reconcile); src/execution/reconciler.py:299-318; src/marketdata/bybit/rest.py:67-90
**Detail:** Coordinator.on_ws_reconnect (coordinator.py:108) acquires the Coordinator RLock, then calls reconciler.reconcile (line 118), which acquires the Reconciler.Lock (reconciler.py:299). Both locks are held simultaneously. Inside reconcile, _fetch_exch_qty / get_open_orders / get_order perform REST calls via _retry_with_backoff (rest.py:67), which on rate-limit codes sleeps base*2^attempt for up to 5 attempts = cumulative ~15.5s of time.sleep (rest.py:76,86) plus network round-trips — all while both locks are held. During that window the pybit WS thread's on_order_event is blocked on the Coordinator RLock. on_order_event handles the SL-trigger sibling-cancel, whose timing window is the 0ms Bybit Spot Triggered->Filled gap (documented coordinator.py:280). Blocking that path for many seconds lets an orphan TP self-fill -> phantom short on Spot. There is no lock CYCLE (no path acquires Reconciler-then-Coordinator), so this is not a deadlock, but it is blocking network I/O under nested locks on the money path. The retry magnitudes make worst-case latency tens of seconds.
**Fix:** Move all exchange-state fetching (get_wallet_balance / get_open_orders / get_order) OUTSIDE the locks: fetch a plain snapshot first, then acquire the lock only to run the pure classify() over that snapshot and apply the verdict transition. Alternatively bound the reconcile-path retries to 1 attempt with a short timeout so the lock-hold window is sub-second. The reconciler classify functions are already pure over (local, exch_qty, open_orders, entry_order), so hoisting the I/O is a localized change.

### [HIGH] TL-02 (trading-logic) — Coordinator safety helpers arm_oco / flatten / reconcile_arming_ttl are dead code from the production path
**File:** src/execution/coordinator.py:402 (arm_oco), 477 (flatten), 594 (reconcile_arming_ttl)
**Detail:** Independent confirmation surface for TL-01. arm_oco (coordinator.py:402), flatten (coordinator.py:477) and reconcile_arming_ttl (coordinator.py:594) have ZERO production call sites (grep src/**.py excluding def/docstrings/log-strings). They are fully implemented and unit-tested in isolation (test_coordinator_arm_oco_*, test_demo_bracket_happy_path) but the only invocations are inside tests that call them directly. This means: (a) the OCO_ARMING TTL watchdog never runs in production so a half-armed bracket can sit in OCO_ARMING indefinitely without the HALT_OCO_ARM_TIMEOUT safety net; (b) the emergency flatten cascade (cancel_all → wallet-read → step-floored Market Sell → retry → HALT_FLATTEN_FAILED) is never reachable from the live loop or from reconcile divergence handling in on_ws_reconnect (which sets halt but does not call flatten). The wiki (components/coordinator.md, runtime-manager.md) documents these 8 methods as the live surface, masking the gap.
**Fix:** Either wire these into the runtime/event flow (preferred — see TL-01) or, if intentionally deferred for v0.1, add explicit 'NOT WIRED — scaffolding for δ activation' banners in coordinator.md / runtime-manager.md and an xfail integration test documenting the gap so it is not mistaken for working safety machinery. Schedule reconcile_arming_ttl into the tick cadence and flatten into both the FLAT-signal path and the RECONCILE_DIVERGENCE branch.

### [HIGH] QS-1 (quant-stats) — DSR units mismatch — per-trade candidate Sharpe vs annualized-fold sigma_sr forces DSR≈0 (false-negative gate), ACTIVE for atr_breakout
**File:** src/analytics/dsr.py:103,139,150 (callers: src/backtest/research_wfa.py:291,312; src/backtest/wfa_reporter.py:81-86)
**Detail:** compute_dsr computes its internal candidate Sharpe as a PER-TRADE (un-annualized) log-return Sharpe: dsr.py:103 `sharpe = mean/std` over compute_returns(log(1+pnl_pct)). But every caller supplies sigma_sr derived from ANNUALIZED fold OOS Sharpes (each runner computes fold sharpe = (mean/std)*sqrt(bars_per_year/mean_holding)). Bailey eq.12 then builds sharpe_star = benchmark + sigma_sr*((1-gamma)z1+gamma z2) on the annualized scale (dsr.py:139), while the in-formula sharpe is per-trade. z_dsr = (sharpe - sharpe_star)*sqrt(n-1)/denom (dsr.py:150) is dominated by the scale gap, not by edge. PROVEN with live data: data/cross_trial_sharpes.json has 7 atr_breakout entries (>=3 => S51-D5 CLASS_SCOPED branch is ACTIVE, not the INSUFFICIENT fallback). Their stdev = 31.75 (annualized); with n_trials=10 => sharpe_star ≈ 49.99. A synthetic genuinely-good strategy (per-trade mean +0.01, sd 0.02, n=60) gives DSR=1.0 at n_trials=1 but DSR=0.000000 with sigma_sr=31.75 (the live path), and DSR=1.0 again if sigma_sr is supplied on the matching per-trade scale (0.05). This is SYSTEMIC: the same conflation exists in wfa_reporter.py:81-86 (donchian/mean-reversion path) whose own docstring lines 5-10 enumerate 3 Sharpe series 'MUST NOT conflate' yet the DSR call mixes Series-2 (per-trade internal) with sigma_sr from Series-1 (bar-returns annualized). It is the un-fixed root of the S50 'DSR=0.0 artifactual' observation — S51 D5 fixed only cross-family contamination, not the scale. No test catches it (test_supertrend_runner.py dsr:0.97 injects a MOCK envelope value, never exercising real compute_dsr with n_trials>1 + realistic fold sharpes). Severity is HIGH not BLOCKER because the failure is fail-closed (DSR≈0 rejects, never admits a bad strategy = no money-path risk) and atr_breakout currently fails other gates anyway — but it defeats the DSR gate's entire purpose (false-negative gate that can NEVER PASS for any class with >=3 entries) and reports a meaningless DSR to the operator/dashboard.
**Fix:** Put SR, SR*, and sigma_SR on ONE scale (Bailey & Lopez de Prado assume a single observation frequency throughout eq.12+13). Either: (a) source sigma_sr from PER-TRADE (un-annualized) fold/trial Sharpes so it matches compute_dsr's internal per-trade sharpe — i.e. store un-annualized trial Sharpes in CrossTrialLog; OR (b) annualize the candidate sharpe inside compute_dsr (multiply by the same sqrt(bars_per_year/mean_holding)) before subtracting sharpe_star. Option (a) is cleaner and aligns with the per-trade returns already used. Add a TDD test that feeds a profitable strategy with n_trials>1 and a realistic same-scale sigma_sr and asserts DSR can exceed 0.95. Reconcile ADR 0056 (lines 124-134 already flag the 3-Sharpe distinction but never reconciled compute_dsr's units).

### [HIGH] DI-01 (data-integrity) — Multi-interval WS gap synthesizes only ONE GAP bar — silent multi-bar data hole
**File:** src/marketdata/bar_builder.py:53-58, 91-109
**Detail:** process_with_gap_fill emits exactly one synthetic GAP bar at (_last_confirmed_open_ms + interval) regardless of how many intervals are missing. Verified by probe: a WS jump T0->T4 (3 missing intervals) produces 1 GAP marker covering only T1->T2; bars at T2 and T3 are silently absent from the persisted series. This violates the brief's gap invariant ('never silently fill-forward without flag' — here it is worse: bars are missing entirely, not even flagged). A downstream consumer reading this Parquet sees a lookback window with collapsed/missing bars -> corrupted indicator math. Severity is HIGH not BLOCKER because the WS->Parquet pipeline (MarketDataPipeline, ParquetBarWriter, BybitWSConsumer, BarBuilder) is currently DORMANT — verified zero instantiations in src/ outside its own modules; the live runtime uses src/runtime/bar_source.py REST-poll instead. The hole is also recoverable on restart via _seed_gaps() REST backfill (gaps.py), but persists during a live run.
**Fix:** In process_with_gap_fill, loop from (_last_confirmed_open_ms + interval_ms) up to (open_ms - interval_ms) in interval_ms steps, emitting one _synth_gap_bar per missing slot, and return list[Bar] (or change the contract to yield all gap bars) instead of a single gap_bar. Update pipeline.py:54-58 to append each. Add a test that asserts N missing intervals -> N GAP markers.

### [HIGH] DI-02 (data-integrity) — BarSource.poll() can emit the currently-forming (unclosed) bar to the strategy as is_closed=True
**File:** src/runtime/bar_source.py:66-72, 78-87 (+ src/marketdata/bybit/rest.py:212,228)
**Detail:** poll() fetches a window start_ms = now - 2*step .. now and takes bars[-1] with no closed-bar guard. get_klines (rest.py:228) hardcodes is_closed=True for EVERY kline including the current in-progress interval, and its filter (rest.py:212) only drops open_ms >= end_ms, so the forming candle (open <= now) passes through. Bybit V5 REST get_kline returns the forming interval as the most-recent element, so when polled mid-interval bars[-1] is a bar whose 'close' is the live mid-interval price (not the settled close) and whose close_time is in the future. KronosStrategy/EMA strategies act on it via on_bar with is_closed=True -> confirm-gate violation ('only is_closed=True bars propagate ... never for strategy', brief CRITICAL) and look-ahead on the live money path. Dedup-by-close_time (line 69) only suppresses RE-emission of the same close_time; it does not prevent the first emission of an incomplete forming bar. The runtime ($_poll_bar_and_strategy manager.py:293-313) trusts the bar unconditionally.
**Fix:** Have BarSource drop the last element when its close_time > now (i.e. select the newest bar whose close_time <= now), OR fetch with end_ms = now and require close_ms < now before returning. Equivalently, stop hardcoding is_closed=True in rest.py and have BarSource only forward closed bars. Add a test feeding a 2-bar window where the newest bar's close_time is in the future and assert poll() returns the prior (settled) bar.

### [HIGH] SEC-S55-01 (security) — Path traversal: attacker-controlled symbol f-string-interpolated into parquet read path, reachable from unauthenticated /api/backtest
**File:** src/__main__.py:456
**Detail:** `_load_ohlcv` builds `parquet_path = f"data/{symbol}_{interval_label}.parquet"` (line 456) with NO allowlist/sanitization of `symbol`. This is reachable end-to-end from the unauthenticated `POST /api/backtest` route. `BacktestPayload` (src/dashboard/app.py:64) declares `symbol: str` with no field_validator, and `run_backtest` (src/dashboard/backtest_runner.py:919-927) validates `req.interval in BARS_PER_YEAR` (closed set) but NEVER validates `req.symbol`. The route-level guards in post_backtest (app.py:202-229) only fire when the preset carries `locked_symbol`/`locked_interval`/`supported_combos` — the presets `ema_crossover`, `mean_reversion`, `mean_reversion_soft`, `donchian` (backtest_runner.py:59,81,102,123) have NONE of those keys, so `payload.symbol` flows unconstrained into the default WFA path -> `_load_ohlcv` (called at backtest_runner.py:1155). Sink confirmation: `data_collector.load_market_data` -> `_resolve_path` (src/backtest/data_collector.py:29-30) only does `os.path.abspath` and does NOT contain to the data dir, then `_read_parquet` (line 66-69) reads it. So `symbol='../../../../some/path/x'` yields an arbitrary absolute path read. Attacker model / severity bound: READ-ONLY (pd.read_parquet) -> information disclosure, not RCE; the `_<interval>.parquet` suffix is forced and a non-parquet/non-OHLCV target raises (then falls back to a fixed CSV path), so full content exfiltration is limited to valid OHLCV-shaped parquet files, but file existence/probing is broader. The dashboard is localhost-only no-auth (app.py:3), and the JSON request body requirement defeats classic drive-by CSRF (cross-origin JSON POST triggers a CORS preflight that fails with no CORS middleware), so realistic exploitation requires local/same-host access. Same vulnerability class and severity as the S48 get_run run_id traversal (which was rated HIGH and fixed). The write-side sibling at src/__main__.py:328 (`Path(f"data/{symbol}_{interval_label}.parquet")`) is CLI-only (`backfill` argv `--symbol`, operator-trusted) and is NOT a finding.
**Fix:** Validate `symbol` against a strict allowlist BEFORE constructing the path, mirroring the existing `_RUN_ID_RE` pattern in backtest_runner.py. Either: (a) add a `@field_validator('symbol')` to BacktestPayload that enforces `re.compile(r'\A[A-Z0-9]{1,20}\Z').fullmatch(v)` (422 at the boundary), or (b) gate in `_load_ohlcv` / `run_backtest` with the same regex and raise ValueError on miss. Allowlist-anchored regex (\A...\Z or .fullmatch) is required — substring `re.match(r'^...$')` would still permit a trailing-newline/path-suffix bypass.

### [HIGH] DASH-01 (dashboard) — Kronos RAW_PRETRAIN_LEAKAGE_SUSPECTED verdict misrenders as a fully-failed WFA acceptance gate (research-vs-WFA dispatch checks `=== 'RAW'` only)
**File:** src/dashboard_react/src/components/metrics/MetricsTable.tsx:349 (and src/dashboard_react/src/components/tabs/HistoryTab.tsx:252,264-269)
**Detail:** Kronos cached + no-cache + variant-miss paths all emit `verdict = "RAW_PRETRAIN_LEAKAGE_SUSPECTED"` (src/dashboard/_kronos_dispatch.py:272,314 + research_runner_envelope build with verdict_override=VERDICT_RAW_PRETRAIN_LEAKAGE). The Kronos `metrics` dict is research-shaped (`{sharpe,win_rate,total_pnl_pct,n_trades}`, research_runner_envelope.py:150-155) with NO `t1_sharpe_oos`/`t5_n_trades`/WFA keys, and `dsr_pass`/`mc_p_value` are null. MetricsTable.tsx:349 dispatches to the reduced honest RawMetricsTable ONLY when `result.verdict === 'RAW'`; because the Kronos verdict string differs, it falls through to WfaMetricsTable (line 352), which reads `m.t1_sharpe_oos` etc. (all undefined → '—'), `result.dsr_pass` (null → FAIL), `result.mc_p_value` (null → FAIL) and renders a full T1-T6/DSR/MC gate grid showing every gate FAILED for a result that was never gated. This directly contradicts the RAW_PRETRAIN_LEAKAGE honesty requirement (ADR 0068/0069 Q4 — exploratory, not a gate). HistoryTab.tsx:252 has the identical root cause: `isRaw = run.verdict === 'RAW'` is false for Kronos, so the SHARPE column reads `m.t1_sharpe_oos` ('—'), N TRADES reads `m.t5_n_trades` ('—'), PnL column shows `run.dsr` (null → '—') instead of `total_pnl_pct`, and MC shows `run.mc_p_value` ('—') — a cached Kronos run displays all dashes despite real sharpe/n_trades/total_pnl_pct living in `m.sharpe`/`m.n_trades`/`m.total_pnl_pct`. types.ts Verdict union and VerdictPanel.tsx:15 DO handle RAW_PRETRAIN_LEAKAGE_SUSPECTED correctly, so only these RAW-vs-WFA dispatch sites were missed. No test feeds a RAW_PRETRAIN result with populated research metrics through these components (test gap). tsc/eslint clean — purely a logic defect.
**Fix:** Treat RAW_PRETRAIN_LEAKAGE_SUSPECTED as a research (non-gated) verdict at every RAW dispatch site. In MetricsTable.tsx:349 use a set: `if (result.verdict === 'RAW' || result.verdict === 'RAW_PRETRAIN_LEAKAGE_SUSPECTED') return <RawMetricsTable .../>`. In HistoryTab.tsx:252 set `const isRaw = run.verdict === 'RAW' || run.verdict === 'RAW_PRETRAIN_LEAKAGE_SUSPECTED'`. Add a shared `RESEARCH_VERDICTS` set to avoid drift. Add a unit test rendering a RAW_PRETRAIN response (research-shaped metrics) through both components asserting the reduced view + populated sharpe/n_trades/pnl cells.

### [HIGH] BYBIT-02 (bybit-api) — Emergency flatten attempt-1→attempt-2 is NOT idempotent (different orderLinkIds) — lost-response double Market Sell
**File:** src/execution/coordinator.py:505-511 (flatten) + 553-582 (_try_place_market_sell)
**Detail:** flatten() places attempt 1 = `_try_place_market_sell(qty, link_id=_emg_link_id(key,1))` and, on False, attempt 2 = `_try_place_market_sell(qty-step, link_id=_emg_link_id(key,2))` — TWO DIFFERENT orderLinkIds (emg attempt 1 vs 2). `_try_place_market_sell` returns False on any non-110072 exception, including a network exception (connection reset / read timeout) after the order was already accepted server-side. Critically, `_retry_with_backoff` does NOT retry network exceptions (rest.py:68) — they propagate. So the sequence: attempt 1 sends a Market Sell, Bybit accepts and executes it, the response read fails → place_order raises → returns False → attempt 2 fires with a DIFFERENT link_id and qty → Bybit has no dedupe basis → a SECOND real Market Sell executes → oversell / phantom short on Spot. The S49 B1 deterministic-orderLinkId fix only makes INTRA-attempt retries (same link_id within _retry_with_backoff) idempotent; it does not cover the inter-attempt (1→2) path. Existing test `test_emergency_flatten_genuine_error_still_halts` only exercises an immediate 110007 (no order landed); the lost-response-after-landing case is untested.
**Fix:** Before placing attempt 2, re-read wallet/position (or call get_open_orders/get_order on the attempt-1 orderLinkId) to confirm the residual is still unsold; skip attempt 2 if already flat. Alternatively, distinguish 'definitely-not-sent' failures (synchronous param/balance retCode = safe to retry) from 'unknown-outcome' failures (network exception = order may have landed → do NOT auto-retry; HALT for operator). At minimum, make _try_place_market_sell return a tri-state (sent-ok / definitely-not-sent / unknown) so flatten only retries on definitely-not-sent. Add a regression test: attempt 1 raises a network exception after the order landed → assert no second sell with a different link_id.

### [HIGH] BYBIT-03 (bybit-api) — Two unrelated BybitAPIError classes — rate-limit-exhaustion error escapes adapter handler and 110072 idempotency check
**File:** src/execution/coordinator.py:369, 559 (except BybitAPIError) vs src/marketdata/bybit/rest.py:93-99
**Detail:** There are TWO distinct BybitAPIError classes: src.execution.bybit.adapter.BybitAPIError (ret_code, ret_msg, .reason) and src.marketdata.bybit.rest.BybitAPIError (ret_code, ret_msg — NO .reason). Verified by MRO: both subclass RuntimeError directly and neither subclasses the other (issubclass both False; isinstance False). adapter.place_order delegates to rest._retry_with_backoff, which on rate-limit EXHAUSTION raises rest.BybitAPIError (rest.py:87-90), not adapter.BybitAPIError. Coordinator flatten paths catch `except BybitAPIError` using the ADAPTER class (import at coordinator.py:29). Consequences: (1) a rest.BybitAPIError from exhausted 170005/170222 retries is NOT caught by these handlers and has no .reason, so the 110072-duplicate success short-circuit (lines 376, 565) is unreachable whenever Bybit's dedupe surfaces as repeated-rate-limit-then-exhaust rather than a single synchronous 110072 — the exact in-flight-duplicate scenario the design is meant to survive; (2) the rest.BybitAPIError falls through to the generic `except Exception`, which for _try_place_market_sell returns False → feeds the attempt-2 double-sell path (BYBIT-02). The generic catch prevents a crash but the intended retCode-based control flow is silently bypassed.
**Fix:** Unify the exception hierarchy: make rest.BybitAPIError the base and have adapter.BybitAPIError subclass it (or re-wrap rest errors at the adapter boundary into adapter.BybitAPIError with a mapped .reason). Ensure every adapter method that calls _retry_with_backoff catches rest.BybitAPIError and re-raises as adapter.BybitAPIError(reason=map_error(code,msg)). Then add 170005/170222 to errors._MAP (currently they map to UNKNOWN_ERROR) so an exhausted-rate-limit error carries RATE_LIMIT_HIT. Add a test: place_order under sustained 170222 to exhaustion → assert the coordinator receives an adapter.BybitAPIError with .reason set.

### [MEDIUM] ARCH-03 (architecture) — RuntimeManager reads Coordinator._repo directly, bypassing the Coordinator public API and its RLock
**File:** src/runtime/manager.py:322, 383 (also coordinator.py:585)
**Detail:** RuntimeManager._poll_bar_and_strategy (manager.py:322) and _shutdown (383) call self._coordinator._repo.get(symbol) — reaching into the Coordinator's PRIVATE _repo to read FSM state from the main tick thread, outside the Coordinator RLock. The inline comment admits the cause: 'no public current_state() on Coordinator'. This is a TOCTOU on the FLAT precheck (line 323) before start_bracket (line 353): between the unlocked read and the locked start_bracket, the WS thread could in principle mutate state. In practice the FSM self-defends (FLAT has no inbound order-event transition; a stale echo from FLAT is dropped by on_order_event's IllegalTransitionError handler), so it is not currently exploitable — but it is a fragile encapsulation break that a future state-graph edit could turn into a crash, and it couples the runtime layer to Coordinator internals. Separately, coordinator.py:585 reads self._adapter._filters.step_size — a private attribute leak across the adapter boundary.
**Fix:** Add a public Coordinator.current_state(symbol) -> ExecutionState | None accessor that reads the row under self._lock, and have RuntimeManager call that instead of _repo.get. Add a public step_size property on the Bybit adapter and have _qty_step() use it instead of _adapter._filters.step_size.

### [MEDIUM] TL-03 (trading-logic) — atr_breakout streaming strategy checks exit priority OPPOSITE to its WFA backtest runner
**File:** src/signalgen/atr_breakout_strategy.py:299-325 vs src/backtest/atr_breakout_runner.py:234-247
**Detail:** The streaming ATRBreakoutStrategy.on_bar checks Priority 1 = reverse-breakout (close[-2] < close[-3] - mult*atr[-2], EXIT_FLAT_ATR_REVERSE) THEN Priority 2 = ATR stop (bar.low <= entry_close - stop_mult*atr, EXIT_FLAT_ATR_STOP_AB). The backtest _backtest_single checks the OPPOSITE order: ATR stop FIRST (low[i] <= stop_price, line 236) then the reverse-breakout exit (elif exit_[i], line 242). When both conditions are true on the same bar the two paths diverge: different reason code AND different fill price (intrabar stop_price vs next-bar open[i]). The WFA-validated artifact (ADR 0060/0064) is the runner, so the streaming strategy does NOT faithfully reproduce the validated edge. Currently low blast radius because the streaming strategy is not wired into live (TL-01) and the live strategy is mean-reversion — but it is a real backtest-vs-live parity defect that would mis-execute if this strategy were ever activated.
**Fix:** Make the streaming strategy mirror the runner's exit priority (ATR stop first, then reverse breakout) — or, if intrabar-stop-first is wrong and signal-flip-first is intended, fix the runner and re-run WFA under a new ADR amendment. Add a parity test (tests/unit/test_atr_breakout_parity.py extension) that feeds a bar where both exit conditions fire and asserts identical reason + fill between streaming and vectorized paths.

### [MEDIUM] TL-04 (trading-logic) — volume_breakout streaming strategy checks exit priority OPPOSITE to its WFA backtest runner
**File:** src/signalgen/volume_breakout_strategy.py:173-195 vs src/backtest/volume_breakout_runner.py:160-173
**Detail:** Same class of defect as TL-03. Streaming VolumeBreakoutStrategy checks Priority 1 = channel exit (close[-2] < channel low, EXIT_FLAT_VOLUME_CHANNEL) THEN Priority 2 = ATR stop (EXIT_FLAT_ATR_STOP_VB). The backtest runner checks ATR stop FIRST (low[i] <= stop_price, line 160; docstring even states 'ATR stop checked FIRST (research/backtest_v2.py line 140)') then channel exit (elif channel_exit, line 167). On a same-bar double-trigger the exit reason and fill price (stop_price vs open[i]) diverge, so the streaming strategy does not reproduce the WFA-validated (ADR 0059) backtest. Blast radius limited because the strategy is not wired into live.
**Fix:** Align the streaming strategy's exit priority with the runner (ATR stop first) and add a same-bar double-trigger parity test asserting identical reason + fill across streaming and vectorized paths. Reference ADR 0059 for the authoritative order.

### [MEDIUM] DI-03 (data-integrity) — Inconsistent migration zero-padding makes lexicographic order != intended order (FK file runs before its referenced table)
**File:** migrations/0006_trade_fills.sql:7-19 (ordering vs migrations/002_risk.sql)
**Detail:** init_db applies migrations via sorted(glob('*.sql')) (db.py:41). Filenames mix 3-digit (001/002/003/004) and 4-digit (0003/0004/0005/0006) prefixes, so Python sorts them: 0003, 0004, 0005, 0006, 001, 002, 003, 004. Thus 0006_trade_fills.sql (FOREIGN KEY parent_trade_id REFERENCES trade_history) is applied BEFORE 002_risk.sql which CREATEs trade_history. Verified: this currently SUCCEEDS on a fresh DB only because SQLite resolves FK references lazily (the referenced table need not exist at CREATE TABLE time, only at INSERT time), and foreign_key_check returns empty when tables are empty. It is a latent hazard: any future migration that performs a non-lazy operation across the misordered boundary (e.g. INSERT INTO a dependent table, or a CHECK/trigger referencing data) will break silently or apply in the wrong order. NOTE: the migration RUNNER atomicity is sound — I verified through the real init_db path that a SQL error mid-migration rolls back the whole transaction (CREATE does not leak, no tracking row, re-run after fix recovers); test_migration_runner_atomicity.py passes. This finding is purely about filename ordering.
**Fix:** Rename future/new migration files to a single consistent zero-padded scheme (e.g. all 4-digit: 0001..0006) so lexicographic order matches dependency order, OR switch init_db to sort by a parsed integer version prefix rather than raw string. Do NOT rename already-applied files in a way that changes schema_migrations keys without a tracking-row migration. Add an assertion in init_db that the dependency-bearing migrations apply in a deterministic, dependency-respecting order.

### [MEDIUM] DI-04 (data-integrity) — Bar model does not reject tz-naive open_time/close_time — tz invariant unenforced at the domain boundary
**File:** src/marketdata/models.py:25-26, 36-44
**Detail:** The Bar pydantic model declares open_time/close_time as plain datetime and the _ohlc_invariants validator checks high/low and close_time>open_time but never asserts tzinfo is not None / is UTC. Probe confirms a tz-naive datetime is ACCEPTED. The brief CRITICAL requires UTC timestamps with no naive datetime. All current construction sites pass UTC (bar_builder, rest.py, kronos_runner use tz=UTC / replace(tzinfo=UTC)), so this is not an active corruption today, but it is the exact gap that makes the Kronos cache-key parity fragile: a naive datetime's .timestamp() is interpreted in the local system tz, which would shift bar_close_ts by the local UTC offset (probe: 10800s on a UTC+3 host) and cause a 100% silent cache MISS (no trades, no error). The domain model is the correct place to make this impossible.
**Fix:** Use pydantic AwareDatetime for open_time/close_time (as trade_history.py/fill_history.py already do for entry_ts/exit_ts), or add a model_validator that raises if open_time.tzinfo is None or open_time.utcoffset() != timedelta(0). Add a test asserting a naive datetime is rejected.

### [MEDIUM] TQ-01 (test-quality) — 3 adapter tests assert ImportError but LOAD THE REAL MODEL when torch+submodule present (side-effecting unit tests)
**File:** tests/unit/test_kronos_adapter.py:73-78, 81-99, 102-113
**Detail:** test_model_adapter_raises_clean_importerror_without_torch, test_model_adapter_syspath_rolled_back_on_importerror, and test_model_adapter_accepts_variant_and_raises_two_step_hint all wrap KronosModelAdapter(variant=...) in pytest.raises(ImportError). With torch 2.12 installed AND third_party/kronos submodule initialized (the operator's real config), __init__ does NOT raise — it SUCCEEDS and instantiates the real Kronos model. I confirmed empirically: KronosModelAdapter(variant=KRONOS_MINI, device='cpu') returned 'INSTANTIATED OK in 1.1s', reading weights from the HF cache (on a cold cache this hits the network). A unit test silently performing model load + potential network I/O is a real test hazard, not merely an env artifact. These are 3 of the 6 torch-present failures. The design flaw: the tests assert on environment state (deps absent) via the negative path only, with no skip-guard and no FORCED-absent setup, so they invert into uncontrolled side effects when the assumption is violated.
**Fix:** Force the dependency absent inside the test rather than relying on env: monkeypatch.setitem(sys.modules,'torch',None) (and/or remove third_party/kronos from sys.path) before constructing the adapter, so ImportError is deterministically raised regardless of installed deps. Alternatively split into env-conditional positive/negative variants. NEVER allow a unit test to instantiate KronosModelAdapter with live torch+submodule (real model load).

### [MEDIUM] TQ-02 (test-quality) — torch-isolation skip-guard is dead code: hard assert fires before pytest.skip
**File:** tests/unit/test_ml_optional_dep.py:16-27
**Detail:** test_core_imports_without_torch executes `assert "torch" not in sys.modules` on line 19 FIRST, then only on line 22-25 tries `import torch; pytest.skip(...)`. When torch is installed in the env (the operator's `.[ml]` config), line 19 fails before the graceful skip can run, so the intended 'skip gracefully if torch installed' logic is unreachable. Confirmed as one of the 6 torch-present failures. The author's intent (skip when torch present) is correct but the statement ordering defeats it.
**Fix:** Move the skip decision before the assertion: at module level use `pytestmark = pytest.mark.skipif(importlib.util.find_spec('torch') is not None, reason='torch installed; isolation N/A')`, OR put the try/import-torch/skip block at the very top of the function body before any assert. This makes the isolation test green in both env configurations.

### [MEDIUM] TQ-03 (test-quality) — test_module_is_torch_free has no skip-guard at all — fails whenever torch is importable
**File:** tests/unit/test_prediction_cache.py:154-159
**Detail:** test_module_is_torch_free does `import src.ml.prediction_cache` then bare `assert "torch" not in sys.modules`, with zero conditional guard (unlike test_ml_optional_dep which at least attempts a skip). Confirmed failing under torch 2.12: AssertionError because torch is genuinely present in sys.modules from the installed package. The test's real intent — 'importing prediction_cache must not pull in torch' — cannot be expressed by checking global sys.modules when torch is installed for other reasons; it needs subprocess isolation or an import-graph (AST) check.
**Fix:** Either (a) run the import in a fresh subprocess with torch made unimportable and assert it imports clean, or (b) replace the runtime sys.modules check with a static AST import-gate over src/ml/prediction_cache.py asserting no top-level `import torch`/`from torch` (the durable pattern already used in test_ml_optional_dep Test B). The AST gate is env-independent and survives torch being installed.

### [MEDIUM] TQ-04 (test-quality) — Behavioral dispatch test polluted with env-state assertion unrelated to the behavior under test
**File:** tests/unit/test_kronos_dispatch.py:128-159
**Detail:** test_run_kronos_dispatch_no_manifest_no_crash exists to verify the no-manifest 'cache not built' path returns an honest structured result. But it embeds `assert "torch" not in sys.modules` at line 135 (precondition) and line 159 (postcondition). With torch installed these assertions fail even though the dispatch logic under test works perfectly — confirmed as the 6th torch-present failure. This conflates two orthogonal concerns (torch isolation vs dispatch behavior); a real regression in the no-manifest path would be indistinguishable from the env-state false failure, and the only CI config (torch-free) hides it so the torch-PRESENT path the operator runs is never validated green.
**Fix:** Remove the two `assert "torch" not in sys.modules` lines from this behavioral test. Keep torch-isolation as a single dedicated AST/subprocess test (TQ-03 fix). Let the dispatch test assert only on the dispatch result (verdict, run_id, kronos_cache_absent warning code).

### [MEDIUM] TQ-05 (test-quality) — C4 integrity anchor _compute_weights_hash has ZERO test coverage and a revision-blind fallback
**File:** scripts/run_kronos_s53.py:157-207
**Detail:** _compute_weights_hash is the cache-integrity defense (C4): it SHA-256s the actual HuggingFace weight files so foreign/stale weights are never reused. grep across tests/ confirms NO test references it (it lives in a script pytest never imports, and the smoke test test_run_kronos_s53_smoke.py does not touch it). Worse, the fallback path (lines 188-196) returns SHA256('model_id|tokenizer_id') when no weight files are on disk — this is INDEPENDENT of variant.model_revision/tokenizer_revision. So two different pinned revisions, with weights not yet downloaded, produce the SAME weights_hash, and the cache key collides → stale cross-revision predictions get reused. The C4 contract (prediction_cache.py:9-13) explicitly claims 'foreign-weights predictions are never reused', but that claim is untested for the revision dimension. This is the exact S27 failure mode: a correctness/integrity-critical function with no test surviving into ship.
**Fix:** Add direct unit tests for _compute_weights_hash: (1) with a tmp HF-cache containing two distinct fake weight files → digest is a stable 64-hex and changes when a byte changes; (2) fallback path (no files) — and FIX the fallback to fold variant.model_revision+tokenizer_revision into the seed so distinct revisions never collide; add a regression test asserting two variants with different revisions but absent weights produce DIFFERENT hashes. Refactor the function out of the script into a torch-free importable module so pytest can reach it.

### [MEDIUM] TQ-06 (test-quality) — Runner net-PnL arithmetic is never asserted by value — only trade count/shape are checked
**File:** src/backtest/kronos_runner.py:250-251, 273-274
**Detail:** The money-path formula `pnl_gross=(fill-entry_price)/entry_price; pnl_net=pnl_gross-2.0*_COMMISSION_TAKER` (and the mark-to-market variant at 273-274) is the headline number that determines the exploratory result. Reviewing test_kronos_runner.py: tests assert trade COUNT (>=1 / ==1), structural shape (entry_price/exit_price/pnl_pct keys), entry_price LOCATION (next-bar open[i+1] vs open[i], lines 301-308), and single-trade sharpe=nan (556) — but NO test asserts the exact pnl_pct value against a hand-computed expectation. A sign error, a missing 2x commission doubling, or slippage applied to the wrong side would pass every existing test. This is precisely the bug class S27 found 4 of across 25 sprints (formula correct by structure, wrong by value).
**Fix:** Add test_run_kronos_exploratory_pnl_exact: a deterministic fixture with known entry open[i+1] and exit open[j+1] (or close[-1] for MTM), compute expected pnl_net = (exit*(1-S) - entry*(1+S))/(entry*(1+S)) - 2*0.001 by hand, and assert abs(trade['pnl_pct'] - expected) < 1e-9. One exact value per fill path (signal-exit and mark-to-market) closes the money-path gap.

### [MEDIUM] DASH-02 (dashboard) — MonthlyHeatmap reports each month's PnL as the arithmetic difference of two COMPOUNDED cumulative percentages, not the actual monthly return — systematically inflated the further equity is from 0%
**File:** src/dashboard_react/src/components/charts/monthlyHeatmapUtils.ts:49 (rendered/labelled by MonthlyHeatmap.tsx:1,54,81)
**Detail:** `computeMonthlyData` builds each cell as `ret = last - prevClose` where `last`/`prevClose` are values from the cumulative `equity_pct` series. The backend equity_pct is compounded: `_compound_equity_pct` returns `(Π(1+pnl_j) - 1) * 100` (backtest_runner.py:378-389), and the research path is also cumulative (research_runner_envelope.py:305). Subtracting two compounded cumulative percentages does NOT yield the period return. Example: a strategy at +800% cumulative whose equity moves 800%→850% over a month shows cell `850 - 800 = +50.0%`, but the true monthly return is `(9.50/9.00 - 1)*100 = +5.6%` — a ~9x overstatement. The component title is 'MONTHLY RETURNS' and cells/tooltips are formatted `+X.X%` (MonthlyHeatmap.tsx:28,78,81), so the operator reads these as real monthly returns. The sum of cells telescopes back to the final cumulative (so totals reconcile), masking the per-cell distortion. Distortion is negligible near 0% but large for the high-return atr_breakout/volume_breakout/Kronos presets the dashboard is specifically built to compare. This is a data-display correctness defect (Axis 3).
**Fix:** Compute monthly return as a ratio of equity multipliers, not a pct-point delta: with `mult_i = 1 + equity_pct_i/100`, monthly return % = `(mult_close / mult_prev_close - 1) * 100` (prev baseline mult = 1.0 for the first month). Update computeMonthlyData accordingly and adjust the MonthlyHeatmap.test.ts expectations. Optionally relabel cells if a pct-point partition is intentionally desired, but the current 'RETURNS %' label requires the ratio form.

### [MEDIUM] DASH-03 (dashboard) — Research-envelope dispatch paths (atr_breakout / volume_breakout / supertrend / kronos) bypass the `_lock`, violating the documented '1 backtest at-a-time' guarantee + non-atomic cache write race
**File:** src/dashboard/backtest_runner.py:1151 (research returns at 995, 1068, 1083, 1148)
**Detail:** The module docstring (line 10) and architecture verdict state 'Concurrency: 1 backtest at-a-time ... Simple threading.Lock'. The lock is only acquired at line 1151, but the atr_breakout (return 1068), volume_breakout (return 995), supertrend (return 1148) and kronos (return 1083) branches all `return` BEFORE reaching `with _lock`. Because POST /api/backtest is registered as a plain `def` (app.py:194, intentionally for threadpool), Starlette runs these handlers in worker threads — two concurrent requests for these strategy types run their runners (pandas/replay CPU work) fully in parallel, breaking the single-flight CPU guarantee the lock was meant to enforce. Additionally, all paths write the result via `cache_path.write_text(json.dumps(...))` (e.g. lines 994, 1067, 1148, 380), which is not atomic; two concurrent requests with identical params (same run_id) can interleave/truncate the same JSON file, and a parallel GET /api/runs/{run_id} (get_run) could read a half-written file → JSONDecodeError 500. Localhost single-user lowers likelihood, but force-recompute double-clicks or the auto-refresh-style polling make it reachable.
**Fix:** Either (a) move the `with _lock:` to wrap the whole run_backtest body (all dispatch branches) so every strategy type is single-flight, matching the docstring; or (b) make cache writes atomic — write to `cache_path.with_suffix('.json.tmp')` then `os.replace()` into place — and acknowledge that research paths run concurrently by correcting the docstring. Atomic write is the minimum fix to prevent torn-file 500s on get_run.

### [MEDIUM] BYBIT-04 (bybit-api) — Residual SL flatten places Market Sell with orderLinkId=None when bracket_id is None — loses retry idempotency
**File:** src/execution/coordinator.py:357-368 (_handle_sl_partial residual_link_id)
**Detail:** In _handle_sl_partial, `residual_link_id` is initialized to None and only set when `row is not None and row.bracket_id is not None` (lines 357-361). bracket_id is `str | None` on ExecutionStateRow (state_repo.py:22). If bracket_id is None, the residual Market Sell at line 363-368 is placed with order_link_id=None → _retry_with_backoff has no idempotency key, so a retry on 170005/170222 after the sell already landed submits a SECOND Market Sell (the exact double-execution class S49 B1 fixed for the populated case). Reachability is low: reaching _handle_sl_partial requires OCO_ARMED, which requires arm_oco to have run, which raises if bracket_id is None (line 419-420) — so in normal flow bracket_id is set. But the None-fallthrough is a latent hole if state is ever seeded/reconciled with a null bracket_id (the emergency flatten() handles this correctly by falling back to self._symbol at line 504; _handle_sl_partial does not).
**Fix:** Mirror the emergency-flatten fallback: when row/bracket_id is None, derive a deterministic key from self._symbol (e.g. make_flatten_link_id(bracket_id=self._symbol[:24], kind="res", attempt=...)) so the residual Sell ALWAYS carries a stable orderLinkId. Never call place_order on a flatten path with order_link_id=None.

### [MEDIUM] BYBIT-05 (bybit-api) — Residual flatten qty (raw leavesQty) not step-floored/fee-adjusted — sub-min dust residual triggers false HALT_FLATTEN_FAILED
**File:** src/execution/coordinator.py:350-368 (_handle_sl_partial leaves_qty)
**Detail:** leaves_qty is taken verbatim from the WS event: `leaves_qty = Decimal(evt.get("leavesQty", "0"))` (line 350) and passed straight to place_order (line 363-368). Unlike the emergency flatten() which step-floors free_qty (lines 493-494) and the entry sizing which fee-adjusts via compute_oco_qty, the residual qty is neither floored to step_size nor checked against min_order_qty before submit. The adapter runs validate_order(qty=leaves_qty) inside place_order (adapter.py:198 via place_order→validate_order), so a residual below min_order_qty raises FilterViolation → caught by the generic `except Exception` at coordinator.py:392 → HALT_FLATTEN_FAILED. A tiny un-sellable dust residual (common after an IOC partial leaves a sub-minimum remainder) therefore halts the executor instead of being treated as effectively-flat. Also, if leavesQty is not an exact multiple of step_size, Bybit can reject with a LOT_SIZE filter error → same false HALT.
**Fix:** Step-floor leaves_qty to qty_step before placing the residual Sell (reuse self._step_floor). If the floored qty is 0 or below min_order_qty, treat as RESIDUAL_FLATTENED (effectively flat / unrecoverable dust) rather than attempting a sell that will be rejected and HALT. Consider fee-aware adjustment consistent with compute_oco_qty if the residual base coin can carry fee dust.

### [LOW] ARCH-05 (architecture) — Layering inversion: lower layers import data-loading + WFA helpers from src.__main__ (the CLI composition root)
**File:** src/backtest/volume_breakout_runner.py:247, 353 (also src/dashboard/backtest_runner.py:1153)
**Detail:** volume_breakout_runner.py:247,353 and backtest_runner.py:1153 do `from src.__main__ import _load_ohlcv` (and _run_wfa_single_symbol). src.__main__ is the CLI entry point / composition root — the top of the dependency stack — yet src/backtest/ and src/dashboard/ depend on its private (underscore-prefixed) functions as if it were a shared library. This is an inverted dependency: the root should consume lower layers, not be consumed by them. It is defused at runtime only because the imports are lazy/in-function (the import-graph probe confirms no import-time cycle), but it is structurally backwards and means OHLCV-loading + single-symbol WFA logic lives in the CLI module instead of in src/backtest/.
**Fix:** Relocate _load_ohlcv and _run_wfa_single_symbol from src/__main__.py into src/backtest/ (e.g. a data_loading module + the existing WFA area), export them as public functions, and have __main__, the dashboard, and the runners import them from src/backtest/. __main__ then becomes a thin CLI wrapper that only orchestrates.

### [LOW] TL-06 (trading-logic) — KronosStrategy is stateless (no _current_side): emits EXIT_FLAT while already flat and can emit ENTRY_LONG while already long
**File:** src/signalgen/kronos_strategy.py:108-156
**Detail:** Unlike every other strategy (EMA/meanrev/donchian/atr_breakout/volume_breakout/supertrend all guard on self._current_side), KronosStrategy.on_bar has no internal position state. It emits EXIT_FLAT_KRONOS on every bar where pred_close < current_close (line 152) even when no position is open, and ENTRY_LONG_KRONOS on every bar where pred_close > current_close*(1+threshold) (line 146) even when already long. This is currently masked by external guards — the live path gates entry on row.state == FLAT (manager.py:323) and the kronos backtest runner guards with in_pos (kronos_runner.py:233,247) — so it produces no incorrect trade today, only redundant no-op signals. The risk is fragility: any future consumer that trusts the signal without an external position guard would double-enter or emit phantom exits, and the inconsistency with the other six strategies is a latent trap.
**Fix:** Add a _current_side state machine to KronosStrategy mirroring the other strategies (only emit ENTRY_LONG when FLAT, only emit EXIT_FLAT when LONG), so the signal contract is self-consistent and does not rely on every downstream consumer re-implementing the position guard.

### [LOW] TL-07 (trading-logic) — EMA / mean-reversion / donchian strategies emit free-form reason string literals instead of ReasonCode enum members
**File:** src/signalgen/strategy.py:142, 155 (also mean_reversion_strategy.py:173,182; donchian_strategy.py:118,128)
**Detail:** strategy.py (EMA) emits reason="ENTRY_LONG_EMA_CROSS_UP" / "EXIT_FLAT_SIGNAL_FLIP"; mean_reversion_strategy.py emits "ENTRY_LONG_MEANREV_RSI_BB" / "EXIT_FLAT_MEANREV_REVERT"; donchian_strategy.py emits "ENTRY_LONG_DONCHIAN_BREAKOUT" / "EXIT_FLAT_ATR_STOP" / "EXIT_FLAT_CHANNEL" — all as raw strings, whereas atr_breakout/volume_breakout/supertrend/kronos correctly use ReasonCode.X.value. These six strings DO currently match enum members added in the S49 ADR 0023 amendment, and RiskManager.assess resolves them via ReasonCode(signal.reason) with a fallback (manager.py:297-300), so there is NO attribution loss today (my prior 'silently collapsed to fallback' note is superseded by the S49 fix). The remaining issue is fragility only: a typo or future rename has no compile-time/test-time link to the enum and would silently fall through to ENTRY_LONG_TREND_FOLLOWING.
**Fix:** Replace the raw strings with ReasonCode.<member>.value in strategy.py, mean_reversion_strategy.py and donchian_strategy.py (matching the newer strategies), and/or add a unit test asserting every strategy's emitted reason resolves to a ReasonCode member without hitting the fallback.

### [LOW] QS-2 (quant-stats) — 4H bars_per_year inconsistent within volume_breakout (2190 used for Sharpe, 2191 reported) and across runners
**File:** src/backtest/volume_breakout_runner.py:39,199 vs 299-300,380-381
**Detail:** volume_breakout_runner uses module constant _BARS_PER_YEAR = 2190 (line 39) for the actual Sharpe annualization in _backtest_single (line 199: trades_per_year = _BARS_PER_YEAR / mean_holding), but the envelope reports bars_per_year = 2191 for interval '240' (lines 299-300 and the WFA path 380-381). So the Sharpe shown to the operator is computed with 2190 while the displayed bars_per_year says 2191. Cross-runner the 4H constant also disagrees: atr_breakout_runner.py:44 uses int(365.25*6)=2191, donchian_runner.py:295 CLI default = 2190, volume_breakout module = 2190. The numeric impact on Sharpe is ~0.05% (sqrt(2191/2190)) — negligible — but it is an internal inconsistency that undermines reproducibility and the L6-unify intent (365.25 family => 2191 should be canonical).
**Fix:** Unify the 4H constant to a single source (365.25*6 -> 2191, or 365*6 -> 2190) and reuse it in both the Sharpe computation and the envelope report. Prefer the 365.25 family (2191) already adopted in atr_breakout and the lookup dicts; change volume_breakout's module _BARS_PER_YEAR from 2190 to 2191 so the computed and reported values match.

### [LOW] DI-06 (data-integrity) — PredictionCache.put() is non-atomic (write_text body then write_text sidecar) — inconsistent with storage.py tmp+os.replace pattern
**File:** src/ml/prediction_cache.py:117-132
**Detail:** put() does path.write_text(body) then sidecar.write_text(_sha256(path)) directly, unlike ParquetBarWriter.append (storage.py:68-83) which stages both to .tmp and os.replace()s them atomically. A crash mid-body-write leaves a truncated artifact; a re-put of the same content-addressed key would then mismatch the (stale) sidecar. Analyzed crash-safety: this is SAFE in effect — a truncated body with no/old sidecar yields a checksum mismatch -> get() returns MISS+UserWarning (verified by tamper probe), never a stale/wrong HIT, so it degrades to a false-miss (re-buildable), not corruption or stale reuse. SPRINT_STATE Carry list already tracks 'prediction-cache put() atomicity' as an open item. Reporting for consistency and to confirm the risk is bounded to false-miss.
**Fix:** Mirror storage.py: write body to <name>.json.tmp, compute sha over the tmp, write <name>.json.sha256.tmp, then os.replace(tmp_body, body) and os.replace(tmp_sidecar, sidecar); unlink tmps in finally. This removes the truncated-artifact window. Low priority since current behavior is fail-safe (MISS), not unsafe.

### [LOW] SEC-S55-03 (security) — Non-atomic cache write: artifact body and SHA-256 sidecar written in separate steps (crash-window leaves unverifiable artifact)
**File:** src/ml/prediction_cache.py:130-132
**Detail:** `put()` does `path.write_text(body)` (line 130) then a separate `sidecar.write_text(_sha256(path))` (line 131-132). A crash/interruption between the two writes leaves an artifact without its sidecar. The read path handles this safely — `get()` (line 144) returns None (miss) when the sidecar is missing, and a tampered/corrupt artifact with a stale sidecar yields a checksum-mismatch miss (line 148-154) with a UserWarning, never trusting bad data. So this is a safe-fail integrity gap, not an exploitable one (also flagged on the SPRINT_STATE Carry list as 'put() atomicity'). Not a money path (cache-replay backtest only). Severity LOW because the failure mode is a cache miss, not silent corruption.
**Fix:** Make the write atomic, mirroring override.py's pattern: write body to a temp file, fsync, write sidecar (or write both into one envelope), then os.replace into place. This keeps the artifact+sidecar pair consistent under crash.

### [LOW] SEC-S55-04 (security) — Full private-WS payload dumped to logs via %r in exception/error paths
**File:** src/execution/bybit/ws_private.py:250,258
**Detail:** `logger.exception('execution event dispatch failed; dropping msg=%r', msg)` (line 250) and `logger.error('order event %s missing required fee fields %s; dropping item=%r', status, missing, item)` (line 258) serialize the entire WS payload into logs. Post-handshake private WS order/fill events carry order data (symbol, qty, price, fees, orderId) but NOT API credentials (authentication occurs at the WS handshake, not in these event payloads — and the only credential-bearing surface, __repr__ at line 58, already redacts to `{api_key[:4]}***`). Pre-existing finding carried from S48. Bounded LOW: no secret exposure today; relevant only as an order-data verbosity concern at mainnet.
**Fix:** At mainnet hardening, bundle with the deferred __repr__/log redaction work: log a bounded summary (orderId, status, symbol) instead of the full `%r` payload, or pass the payload through a field-allowlist redactor before logging.

### [LOW] PY-1 (python) — CI ruff gate threshold of 200 lets real lint regressions land green
**File:** /Users/Apple/Desktop/Vibe_Code/Bot/AI_Traiding_Bot/.github/workflows/ci.yml:83
**Detail:** The CI ruff step (`ruff check src/ tests/`) only fails the build when error count exceeds 200 (`if [ "${ERR_COUNT}" -gt 200 ]`). The current `.venv/bin/ruff check src/ tests/` run produces 112 errors (8 in src/, the rest in tests/) — all pass green. The mypy gate is correctly strict (`-gt 0`, line 100), but the ruff gate is effectively disabled for src/ regressions: up to ~200 new lint violations (including B-class bugbear, SIM, ARG, RET, UP deprecations) could be merged without failing. The inline comment claims a '~169 baseline on main (legacy)', but pyproject per-file-ignores already exclude src/core/*, src/backtest/*, src/risk/risk_manager.py from ruff, so the real un-ignored count is ~8. The 200 threshold is far above the true baseline.
**Fix:** Tighten the threshold to the actual current baseline (8 for src/-only, or run `ruff check src/ tests/` and pin the real count). Better: separate the gates — strict (`-gt 0`) for src/ after clearing config_loader.py, and a tracked baseline for tests/. Mirror the mypy step's `-gt 0` discipline.

### [LOW] PY-2 (python) — config_loader.py uses deprecated typing.Dict + unnecessary open mode (only src/ file failing ruff)
**File:** /Users/Apple/Desktop/Vibe_Code/Bot/AI_Traiding_Bot/src/config_loader.py:2,7,11,18
**Detail:** All 8 of the src/-scoped ruff errors are concentrated in this single file: UP035 deprecated `from typing import Dict` (line 2), UP006 `Dict[str, Any]` annotations that should be `dict[str, Any]` (lines 7, 18), and UP015 unnecessary `"r"` mode arg in `open(config_path, "r", ...)` (line 11). The file is NOT dead code — it is imported by the root-level entry scripts `app.py:457` and `run_backtest.py:10` via `from src.config_loader import load_config`. Because it lives in src/ it is in mypy/ruff scope, but only the loose 200-threshold ruff gate (PY-1) keeps it from failing CI. The project standard is py312 modern typing (PEP 585 builtins) everywhere else.
**Fix:** Run `.venv/bin/ruff check --fix src/config_loader.py` (3 of the 4 are auto-fixable): replace `Dict` with builtin `dict`, drop the `from typing import Dict` import, and remove the `"r"` mode argument. This clears the src/ ruff baseline to 0, enabling PY-1's strict-gate fix.

### [LOW] PY-3 (python) — Wilder _atr() duplicated byte-for-byte between two backtest runners (DRY)
**File:** /Users/Apple/Desktop/Vibe_Code/Bot/AI_Traiding_Bot/src/backtest/volume_breakout_runner.py:53
**Detail:** `_atr(df, period)` in volume_breakout_runner.py:53 and atr_breakout_runner.py:142 are algorithmically identical — a `diff` of the two function bodies shows the ONLY difference is one inline comment line. Both compute TR with prev_close[0]=close[0], SMA-seed at period-1, then the Wilder recursion. Both live under the `src/backtest/* = ["ALL"]` ruff exclusion so the duplication is invisible to lint. A future fix to one (e.g. the prev_close[0] convention, or a NaN-warmup edge) can silently diverge from the other, producing inconsistent ATR/stop sizing across the two strategies. NOTE: the other ATR variants in the tree (signalgen/indicators.wilder_atr, atr_breakout_strategy._WilderATR incremental, supertrend_runner._wilder_atr_vectorized) are intentional by-design (Decimal vs float boundary, incremental O(1) vs vectorized) and are NOT part of this duplication.
**Fix:** Extract a single `_wilder_atr(df, period) -> np.ndarray` into src/backtest/indicators.py (or a shared helper) and import it in both runners. Keep the docstring noting the prev_close[0]=close[0] convention so the contract stays explicit. Add a parity unit test so the two callers can never diverge.

### [LOW] PY-4 (python) — Root-level tracked Python scripts are outside both CI quality gates
**File:** /Users/Apple/Desktop/Vibe_Code/Bot/AI_Traiding_Bot/.github/workflows/ci.yml:78-104
**Detail:** Both CI quality gates are scoped to `src/ tests/` only: ruff runs `ruff check src/ tests/` (line 80) and mypy runs `mypy --strict src/` (line 97). The repo tracks several root-level Python entry points — `app.py`, `run_backtest.py`, `download_btc_data_v4.py`, `conftest.py` — that are neither linted nor type-checked by CI. `app.py` and `run_backtest.py` are real entry points (they import from src/, e.g. `from src.config_loader import load_config`), so type/lint regressions there ship unnoticed. These are technically outside the strict src/ audit scope, but represent a quality-gate coverage gap for executable code.
**Fix:** Either move the real entry points into src/ (e.g. src/dashboard/app.py already exists — consolidate), or extend the CI gates to include the tracked root scripts: `ruff check src/ tests/ *.py` and add the root scripts to mypy's scope. If they are intentionally throwaway research scripts, add them to ruff `extend-exclude` explicitly so the omission is documented rather than accidental.

### [LOW] PY-5 (python) — PredictionCache.put() performs a non-atomic two-step write (artifact then sidecar)
**File:** /Users/Apple/Desktop/Vibe_Code/Bot/AI_Traiding_Bot/src/ml/prediction_cache.py:130
**Detail:** put() writes the JSON artifact with `path.write_text(body)` (line 130) and then writes the SHA-256 sidecar with `sidecar.write_text(_sha256(path))` (line 132) as two separate, non-atomic filesystem operations. A process crash or interruption between the two writes leaves an artifact without a matching sidecar. The degradation is graceful — get() treats a missing sidecar (line 144) or a partial body whose checksum won't match (line 148) as a MISS, never raising — so this is data-loss-safe, not a correctness hazard. This is already a known carry in SPRINT_STATE ('prediction-cache put() atomicity'). The cache-build path is single-process offline (T7 script), so concurrent corruption is not a concern; the only residual is a wasted re-compute on the next build.
**Fix:** Write to a temp file in the same directory and atomically rename into place: `tmp = path.with_suffix('.json.tmp'); tmp.write_text(body); tmp.replace(path)`, then the same for the sidecar — or write the sidecar first and the artifact last so a torn write never leaves a 'valid-looking' artifact. POSIX rename within a directory is atomic. Mirrors the override.py:95 tmp.unlink pattern already used elsewhere in the codebase.

### [LOW] TQ-07 (test-quality) — win_rate and equity_curve_pct (incl. NaN-coercion branch) computed but never asserted in any test
**File:** src/backtest/kronos_runner.py:294-305, 313
**Detail:** run_kronos_exploratory derives win_rate (line 295), the cumulative equity_pct curve (261, 284), equity_curve_pct x100 (305), and a win_rate NaN→0.0 coercion at line 313. grep across all tests confirms zero assertions on win_rate or equity_curve in any kronos runner/dispatch test (win_rate is asserted only for unrelated supertrend/strategy_metrics modules). The NaN-coercion branch (n_trades==0 → win_rate=nan → coerced to 0.0 in the envelope) is never exercised with a check, and the equity-curve additive accumulation (which feeds the dashboard chart) is unverified.
**Fix:** Add assertions to the existing round-trip and cache-miss tests: for the 0-trade case assert envelope win_rate==0.0 (coercion fired) and equity_curve==[0.0]; for a known 2-trade fixture with one winner and one loser assert win_rate==0.5 and that equity_curve_pct is the running cumulative sum x100 of pnl_net. Cheap additions to fixtures that already run.

### [LOW] TQ-08 (test-quality) — Local MockKronosAdapter has a signature divergent from the real KronosAdapter Protocol (mock-drift)
**File:** tests/unit/test_kronos_manifest_v2.py:31-35
**Detail:** The test defines a local MockKronosAdapter with predict(self, context_df, *, lookback, horizon) — keyword-ONLY lookback/horizon and a param named context_df. The real Protocol and src.ml MockKronosAdapter use positional predict(self, ohlcv_df, lookback, horizon) (confirmed via inspect.signature). The local mock passes only because _build_cache_for_combo happens to call with kwargs (kronos_runner/script call sites); if a call site used positional args (as the Protocol permits), the regression would be invisible to this test. This is a mild mock-overuse / mock-drift anti-pattern: the test double does not honor the real contract.
**Fix:** Import and use the real MockKronosAdapter from src.ml.kronos_adapter (it is torch-free and deterministic) instead of redefining a divergent local stub. If a fixed-output double is needed, match the Protocol signature exactly (positional ohlcv_df, lookback, horizon) so it would catch a positional-call regression.

### [LOW] DASH-04 (dashboard) — HistoryTab verdict styling and summary text have no case for RAW_PRETRAIN_LEAKAGE_SUSPECTED → Kronos runs shown in red 'fail' color with a generic summary that drops the look-ahead/leakage caveat
**File:** src/dashboard_react/src/components/tabs/HistoryTab.tsx:21-35 (verdictCellClass) + 85-92 (renderSummary)
**Detail:** `verdictCellClass` (line 21) switch has no `RAW_PRETRAIN_LEAKAGE_SUSPECTED` case, so it hits `default` → `verdictFail` (red), making an exploratory Kronos run look like a failure in the history table — inconsistent with VerdictPanel.tsx:15 which styles it as `verdictRaw`. `renderSummary` (line 85) only special-cases `'RAW'` to emit the 'Подвержен look-ahead bias. Не basis for live decisions' caveat; a Kronos verdict falls to the generic fallback (line 92) `Verdict {verdict}. Total PnL {x}%`, losing the pretrain-leakage honesty message in the expanded-row summary. Same RAW-string-literal root cause as DASH-01.
**Fix:** Add `case 'RAW_PRETRAIN_LEAKAGE_SUSPECTED': return styles.verdictRaw ?? ''` to verdictCellClass, and add a RAW_PRETRAIN branch in renderSummary surfacing the pretrain-leakage + exploratory-only caveat (mirror the backend `pretrain_leakage` warning). Fold into the shared RESEARCH_VERDICTS refactor from DASH-01.

### [LOW] DASH-05 (dashboard) — Stale OPTGROUP_ORDER omits the newer strategy optgroups ('Тренд', 'ML / Прогноз') → supertrend & Kronos groups render in arbitrary (object-key) order after the known groups
**File:** src/dashboard_react/src/components/forms/ConfigureBacktest.tsx:26 (OPTGROUP_ORDER), 99-103 (orderedGroups)
**Detail:** OPTGROUP_ORDER lists only ['Тренд-следование','Возврат к среднему','Прорывы'], but STRATEGY_PRESETS defines supertrend with optgroup 'Тренд' (backtest_runner.py:271) and kronos with optgroup 'ML / Прогноз' (backtest_runner.py:206). orderedGroups appends unknown groups as 'extras' (line 101) in Object.keys() iteration order, so the dropdown still shows all presets (not a functional break) but the ML/Kronos and Supertrend groups appear in an unstable, non-curated position. Note 'Тренд' vs 'Тренд-следование' are two distinct groups for what are both trend strategies — likely an unintended split.
**Fix:** Add 'Тренд' (or consolidate supertrend into 'Тренд-следование') and 'ML / Прогноз' to OPTGROUP_ORDER in the desired display order. Consider deriving the ordering from a single source shared with the backend preset definitions to prevent future drift.

### [LOW] BYBIT-06 (bybit-api) — Kline backfill can stall (no forward progress guard) if Bybit returns a non-advancing batch
**File:** src/marketdata/bybit/rest.py:188-240 (get_klines backward-walk loop)
**Detail:** The backward-walk loop sets `cur_end = oldest_open_ms` each iteration and only terminates via `cur_end > start_ms` (line 188), `not rows`/`not batch_bars` breaks (lines 207, 232-233), or `oldest_open_ms <= start_ms` (line 238). There is no guard that cur_end strictly DECREASES across iterations. If Bybit ever returns a batch whose oldest bar is >= the previous cur_end (e.g. clamped/duplicate page, or a single-bar page where oldest==cur_end), cur_end does not advance and the loop re-issues the identical request indefinitely (with backoff only on rate-limit codes, otherwise a tight REST hammer). Empirically rare given V5 end-anchored semantics, but there is no defensive bound.
**Fix:** Add a strict-progress guard: track prev_cur_end and break (or raise a typed gap error) if `oldest_open_ms >= prev_cur_end` (no advance). Optionally cap total iterations as a backstop. This converts a potential infinite REST loop into a clean termination plus a logged gap for the gap-synthesizer.
