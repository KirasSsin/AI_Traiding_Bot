[pytest]
asyncio_mode = auto
markers =
    demo: opt-in demo integration tests (RUN_DEMO=1)
    integration: slow end-to-end integration tests (opt-in)
    property: Hypothesis property-based invariant tests
